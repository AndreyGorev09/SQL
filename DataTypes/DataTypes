/* Тип данных - множество допустимых значений и совокупность операций над ними. */

1. INTEGER - ПОЛОЖИТЕЛЬНЫЕ И ОТРИЦАТЕДЬНЫЕ ЦЕЛЫЕ ЧИСЛА.

2 .FLOAT(precition, scale) - ДРОБНОЕ ЧИСЛО ГДЕ precition - ОБЩЕЕ МАКСИМАЛЬНОЕ КОЛИЧЕСТВО ЦИФР, scale -
СКОЛЬКО ОТ ДРОБНОГО КОЛИЧЕСТВА ОТВОДИТЬСЯ НА ДРОБНУЮ ЧАСТЬ. МЕНЬШЕ МАКСИМАЛЬНЫХ ПАРАМЕТРОВ ИСПОЛЬЗОВАТЬ МОЖНО.

3. CHAR(length) - СТРОКОВЫЙ ТИП ДАННЫХ, ГДЕ length - КОЛИЧЕСТВО СИМВОЛОВ В СТРОКЕ. ДЛИНА В СТРОКЕ ФИКСИРОВАННАЯ -
КАКАЯ ДЛИНА УКАЗАНА ТАКАЯ БУДЕТ ХРАНИТЬСЯ В ПАМЯТИ.

4. VARCHAR(size) - СТРОКОВЫЙ ТИП ДАННЫХ, ГДЕ size - КОЛИЧЕСТВО СИМВОЛОВ В СТРОКЕ. ДЛИНА В СТРОКЕ ПЕРЕМЕННАЯ -
СКОЛЬКО СИМВОЛОВ НАПЕЧАТАНО В СТРОКЕ СТОЛЬКО БУДЕТ ХРАНИТЬСЯ В ПАМЯТИ.

5. DATE - СОДЕРЖИТ ИНФОРМАЦИЮ О ГОДЕ, МЕСЯЦЕ, ЧАСЕ, МИНУТЕ, СЕКУНДЕ: 2022-11-12 22:29:18

6. TIMESTAMP(f) - СОДЕРЖИТ ИНФОРМАЦИЮ О ГОДЕ, МЕСЯЦЕ, ЧАСЕ, МИНУТЕ, СЕКУНДЕ, ДОЛЯ СЕКУНДЫ, ГДЕ f - УКАЗЫВАЕТ ЧИСЛО
ЦИФР В ДОЛЯХ СЕКУНДАХ: TIMESTAMP(6) = 2022-11-13 22:29:18.126000

7. NULL - ЭТО ОТСУТСТВИЕ ДАННЫХ, ЗНАЧЕНИЕ НЕ ЗАНИМАЕТ МЕСТО В ПАМЯТИ.
РЕЗУЛЬТАТ АРИФМИТИЧЕСКИХ ОПЕРАЦИЙ С NULL ВСЕГДА NULL.


CREATE DATABASE date_type_db;
USE date_type_db;
CREATE TABLE types (
    my_integer INTEGER NOT NULL,
    my_float FLOAT(5,2) NOT NULL,
     my_char CHAR(8) NOT NULL,
     my_varchar VARCHAR(8) NOT NULL,
     my_date DATE NOT NULL,
     my_timestamp TIMESTAMP(4) NOT NULL
     my_null INT NULL);

INSERT INTO types VALUES (1, 123.234556, 'ABCDFG', 'ABCDFG', '2022-11-13 23:47:18', '2022-11-13 23:07:18.1200', NULL);
SELECT * FROM types;








